#include<stdio.h>
void main()
{
	int a,b;
	printf("Enter the value of a&b:");
	scanf("%d  %d", &a, &b);
	if(a>b)
	{
		printf("largest number is %", a);
	}
	else
	{
		printf("largest number is %", b);
		
	}
}
